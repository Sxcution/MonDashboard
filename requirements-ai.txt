# Optional AI image tools for the Image tab.
# Install separately because these packages/models are heavy.

# Object Remove / inpainting backend.
# Install this after requirements.txt. The package metadata is old and asks for
# older numpy/pillow/fire, but the dashboard keeps the newer core stack.
# Recommended command:
#   python -m pip install simple-lama-inpainting
#   python -m pip install -r requirements.txt

# Simple LaMa depends on PyTorch. Install a PyTorch build that matches your machine
# from https://pytorch.org/get-started/locally/ before or after this file.
#
# Upscale/Enhance first uses the installed Upscayl app:
#   C:\Program Files\Upscayl\resources\bin\upscayl-bin.exe
#   C:\Program Files\Upscayl\resources\models\upscayl-standard-4x.*
# Override paths with UPSCAYL_BIN and UPSCAYL_MODELS_DIR if needed.
# OpenCV DNN Super Resolution remains a CPU fallback.
