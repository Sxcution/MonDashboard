// Legacy all-in-one app source has been retired.
// Active pages now load split page scripts from script.ts, dashboard.ts, mxh.ts,
// notes.ts, image.ts, settings.ts, telegram.ts, and chat.ts.
// This marker keeps the generated app.js path stable for compatibility.
